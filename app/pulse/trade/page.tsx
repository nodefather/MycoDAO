import TradingTerminal from "@/components/pulse/TradingTerminal";

export const metadata = {
  title: "Trade | Pulse",
  description: "MycoDAO Pulse trading terminal — charts and broker-backed orders",
};

export default function TradePage() {
  return <TradingTerminal />;
}
